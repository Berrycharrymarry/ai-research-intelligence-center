# AI Research Intelligence Center — backend dependencies
# Install with:  python -m pip install -r requirements.txt
# (In this sandboxed workspace the packages are vendored into backend/vendor via
#  `python -m pip install --no-cache-dir --target backend/vendor -r requirements.txt`
#  and run with PYTHONPATH=backend/vendor; see README.md.)

fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
httpx>=0.27
pytest>=8.0
